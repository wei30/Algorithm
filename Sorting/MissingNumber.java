public class MissingNum{
	while(low <= high){
		mid = low + ((high-low)/2);
		if(arr[mid] == num){
			return mid;
		}
		if(arr[mid <=a[high]){
			if(num > arr[mid] && num <=arr[high]){
				low = mid + 1;
			}
		}
	}
}
